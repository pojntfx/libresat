import { LightLayout } from "../layouts/LightLayout";

export default () => (
  <LightLayout title="Chat">
    <div>
      You will be able to choose between an IRC and Mattermost (Slack-like) chat
      here.
    </div>
  </LightLayout>
);
