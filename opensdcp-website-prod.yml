version: "3"

services: 
  web:
    image: node:latest
    ports:
      - 80:5000
    working_dir: /home/node/app
    volumes:
      - ./:/home/node/app
    command: "npm start"
    deploy:
      replicas: 5
      update_config:
        parallelism: 2
      restart_policy:
        condition: on-failure