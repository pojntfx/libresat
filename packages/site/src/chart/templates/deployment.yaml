apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: web
  namespace: site
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: web
          image: pojntfx/libresat-site
          ports:
            - containerPort: 9000
