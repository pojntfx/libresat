{
  "name": "@libresat/identity",
  "version": "0.0.1-9",
  "description": "Identity provider for [LibreSat](https://libresat.space/)",
  "main": "dist/index.ts",
  "bin": "dist/tasks/start.js",
  "scripts": {
    "dev": "nodemon --ext json,ts,graphql --exec ts-node --project ./tsconfig.json --files src/tasks/start.ts",
    "build": "tsc -p tsconfig.json && cp -r src/typeDefs dist/",
    "build:binary": "pkg package.json --targets node10-linux-x64 --out-path dist/",
    "start": "node dist/tasks/start.js",
    "start:binary": "chmod +x dist/identity && dist/identity",
    "install:binary": "sudo install dist/identity /usr/bin",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/libresat/libresat/tree/master/packages/identity"
  },
  "keywords": [
    "identity",
    "authentication",
    "authorization",
    "roles",
    "users"
  ],
  "author": "Felicitas Pojtinger <felicitas@pojtinger.com>",
  "license": "AGPL-3.0",
  "dependencies": {
    "@libresat/service": "^0.0.1-9"
  },
  "devDependencies": {
    "typescript": "^3.0.3",
    "nodemon": "^1.18.4",
    "pkg": "^4.3.4",
    "ts-node": "^7.0.1"
  },
  "pkg": {
    "scripts": "dist/**/*.js",
    "assets": "dist/**/*.graphql"
  },
  "publishConfig": {
    "access": "public"
  }
}
