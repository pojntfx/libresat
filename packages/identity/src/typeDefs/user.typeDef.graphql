type User {
  _id: String!
  name: String!
  roles: [Role]
}

type Mutation {
  createUser(roleId: String!, name: String!): User
}

type Query {
  user(roleId: String!, id: String!): User
  users(filter: UserFilterInput): [User]
}

input UserFilterInput {
  _id: String
  name: String
}
