version: "3"

services: 
  web:
    image: node:latest
    ports:
      - "8100:3000"
    working_dir: /home/node/app
    volumes:
      - ./:/home/node/app
    command: bash -c "npm install; npm run dev"
