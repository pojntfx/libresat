// Styled components
import styled from "styled-components";

// Style constants
import { white, dialogInnerPadding } from "../constants";

export default styled.div`
  background: rgb(${white});
  padding: ${dialogInnerPadding};
`;
